.DS_Store
**/.DS_Store

__pycache__/
**/__pycache__/
*.pyc

# ZIPs are used as local source bundles; we publish extracted galleries instead
phases/**/*.zip

# Optional local clutter
.venv/
.python-version

# Word/Office temp + intermediates
exports/~$*.docx
exports/*.raw.docx

# Exported chat transcripts (may contain sensitive info)
chats/*.json
chats/*.zip
