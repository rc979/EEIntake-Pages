[build]
  # Publish the docs/ folder as the site root
  publish = "docs"
  # No build command needed (static HTML)
  command = ""

# Redirect rules (optional - for clean URLs)
# Test change: updated redirect rule
[[redirects]]
  from = "/"
  to = "/index.html"
  status = 200

# Ensure .nojekyll is respected (Netlify doesn't need it, but good practice)
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    # Test header to verify netlify.toml is being read
    X-Netlify-Config = "active"

# Password protection configuration
# Note: Password protection must be enabled in Netlify dashboard (not configurable via toml)
# To enable: Site settings → Access control → Password protection → Enable
# Set a password in the dashboard; it will protect the entire site

# Password protection (set password in Netlify dashboard: Site settings → Access control)
# Note: Password protection must be enabled in Netlify dashboard; this file documents the intent.
# To enable: Go to Site settings → Access control → Password protection → Enable
